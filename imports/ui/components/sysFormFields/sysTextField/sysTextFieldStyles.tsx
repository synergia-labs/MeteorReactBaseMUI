import React from 'react';

import { styled } from '@mui/system';
import Box from '@mui/material/Box';
import { read } from 'fs';

interface ISysTextFieldStyles {}

const SysTextFieldStyles: ISysTextFieldStyles = {
	showNumber: styled(Box)({
		marginLeft: 'auto'
	})
};
