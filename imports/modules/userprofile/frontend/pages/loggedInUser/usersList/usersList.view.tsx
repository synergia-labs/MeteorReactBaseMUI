import React, { useContext } from 'react';
import Styles from './usersList.styles';

const UserListView: React.FC = () => {
    // const { user } = useContext(AuthContext)
     
    return (
        <Styles.container>
            {/* <SendEmailVerification user={user!}  url='' /> */}
        </Styles.container>
    )
};

export default UserListView;