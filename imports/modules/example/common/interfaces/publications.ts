//import { PublicationType } from "/imports/base/types/publication"; // Uncomment this line if you are using publications

/*
    You need to declare the module publications here with the same name as the enum/publications.ts (keyName).
    This is necessary to be able to use the publications in the backend/frontend linked with typescript.
*/
export interface IExampleApiPublication {
	// basePublication: PublicationType<typeof basePublication> // Example of publication definition
}
